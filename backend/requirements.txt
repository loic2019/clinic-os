# CLINIC OS — Backend dependencies (Phase 1: Foundation, Phase 2: Auth)

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# --- Cache / async tasks (wired in later phases, installed now) ---
redis==5.2.1

# --- Security (used from Phase 2 onward, installed now) ---
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
python-multipart==0.0.20

# --- HTTP client / testing ---
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0

# --- Utilities ---
python-dotenv==1.0.1
